# 大屏栏目图表化升级 · 说明

## 一句话
把左右两栏从「各一张图」升级为「**左 KPI + 2 图 / 右 3 图**」的完整指标体系，全部数据用最贴合语义的图表类型呈现，后台可逐点编辑并实时联动。

---

## 栏目结构（五个 tab 通用）

| 位置 | 栏目定位 | 内容 |
|---|---|---|
| 左栏 ① | 核心指标 | 3~6 张 KPI 卡：数值 + 单位 + 同比徽标 + 12 点迷你趋势条 |
| 左栏 ② | 核心趋势 | 月度趋势折线（渐变面积、发光线） |
| 左栏 ③ | 区域对比 | 各区县分组柱 / 堆叠柱 |
| 右栏 ① | 构成占比 | 环形图（中心显示合计） |
| 右栏 ② | 排名 | 横向排名条（自动降序、背景槽、值标签） |
| 右栏 ③ | 能力/达成 | 雷达评估 或 仪表达成率 |

## 各板块具体配置

### 综合概览（汇总，只读）
- 左：全市服务总量月度趋势 · 近半年四板块服务量结构（堆叠）
- 右：四大板块服务量占比 · **各区县养老服务综合指数**（床位 0.4 + 设施 0.35 + 改造 0.25 归一化加权）· 全市能力雷达（德阳 vs 全省均值）

### 政府监管
- 左：监管事项月度趋势（检查/整改双线）· 各区县监管强度（检查数 vs 发现问题）
- 右：检查问题类型构成 · 各区县隐患整改率排名 · 年度监管任务完成率（仪表）

### 机构养老
- 左：在院老人与床位使用率趋势（**双 Y 轴**）· 各区县床位供给与在院
- 右：机构类型构成 · 星级评定分布 · 服务能力雷达（本年度 vs 上年度）

### 社区养老
- 左：服务人次月度趋势 · 各区县社区设施与食堂
- 右：服务类型构成 · 长者食堂日均就餐排名 · 设施覆盖率（仪表）

### 居家养老
- 左：上门服务单量趋势 · 各区县居家老人能力分级（自理/半失能/失能/高龄独居 堆叠）
- 右：服务类型构成 · 适老化改造完成排名 · 服务质量雷达

---

## 技术改动

### `src/stores/sections.js`（重构）
- `left` / `right` 改为**图表块数组**，块结构：
  `{ key, title, type, unit, categories, series[{ name, data, axis? }], indicators?, max? }`
- 支持类型：`line | bar | stack | hbar | ring | radar | gauge`；`axis: 1` 走右侧 Y 轴。
- KPI 新增 `spark` 数组驱动卡片迷你趋势条。
- `overview` 仍由四板块 computed 实时汇总，新增综合指数、半年结构、能力雷达三项派生。
- `updatePoint` 签名新增 `blockIdx`。

### `src/views/BigScreen.vue`
- 左右栏改为多面板 `v-for`，ECharts 实例用 `chartEls / chartInsts` 按 `side + 下标` 管理。
- 面板 `key` 不含 tab，切换板块时复用 DOM、只 `setOption`，无闪烁。
- `buildOption` 重写，统一 7 种图表类型；配套 `grad / grad2 / gradH / shortNum`（万/k 简写）。
- 新增 `watch(view, deep)` —— 后台改数据，大屏立刻跟着变。
- 资源管理收敛为 `allPanelInsts()` 统一 resize / dispose。

### `src/views/admin/ScreenData.vue`
- 左栏两列、右栏三列网格，逐图表块渲染编辑表格。
- CSV 导出新增「栏目 / 系列」列，覆盖全部图表块。
- 概览页列出所有汇总图表块清单。

---

## 验证
- `vite build` 通过（902 modules）。
- 预览：http://localhost:5180/ （建议硬刷新 Ctrl/Cmd+Shift+R）。
