# 3D 地图深度重做 — Canvas 2.5D 立体挤出引擎

## 为什么重做
原 3D 地图基于 echarts-gl `geo3D`，存在两个无法靠调参根治的硬伤：
1. **全区统一挤出高度**（`regionHeight` 是单值），所有区县一样高 → 扁平、廉价感。
2. **`realistic` 阴影 + SSAO/Bloom** 调不好就发灰发糊，与"亮青实景"诉求冲突。

环境上 Three.js 装不了（沙箱出网受限），因此新建**零依赖的 Canvas 2.5D 立体挤出引擎**，彻底绕开 geo3D 天花板。

## 新引擎做了什么（`src/utils/map3dEngine.js`）
- **数据驱动起伏**：每个区县按老年人口挤出不同高度（4~24），中江县最高、最直观。
- **霓虹玻璃质感**：顶面半透明渐变 + 发光边（shadowBlur）+ 侧墙渐变，画家算法保证遮挡正确。
- **地面氛围**：扫描环（地面圆随斜投影成椭圆）、中心光晕、各区县落地阴影。
- **光柱标记**：公立医疗/机构/社区/居家/告警/标注按分类配色，顶部脉冲光点 + 悬停标签。
- **可交互**：拖拽旋转、滚轮缩放、空闲自动缓转、悬停高亮 + 提示、点击弹窗（复用原详情组件）。
- 多边形抽稀 + DPR 清晰化，目标 60fps，零外部依赖、100% 可构建。

## 接入方式（`src/views/BigScreen.vue`）
- 新增 `#mapCanvas` 宿主（默认隐藏）+ `#mapTip` 悬停提示。
- 用 `ensureEngine / build3DMarkers / renderMap3D / show2D` 替换原 geo3D 渲染；2D 模式保持 echarts 不变。
- 点击/悬停回调直接驱动既有详情弹窗与提示层。

## 验证
- `vite build` 通过（902 modules，仅 chunk 体积 warning，非错误）。
- 投影数学离线校验：坐标有限、高度随人口变化合理。
- 预览：**http://localhost:5180/** → 切到「3D」查看立体起伏、霓虹边、光柱、扫描环、拖拽旋转/滚轮缩放、悬停与点击交互。

## 后续可微调（如需）
- 相机俯仰/默认方位角、缩放上限、高度比例。
- 区县配色明度、侧墙明暗、扫描环数量与速度。
- 若日后放行 npm，可再升级为真 Three.js（ExtrudeGeometry）获得真实 3D 光照与阴影。
