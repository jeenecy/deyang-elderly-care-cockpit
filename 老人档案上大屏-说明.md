# G03 补完 · 老人档案上大屏（关怀对象板块）

## 目标
将 G03 老人档案接入驾驶舱数字大屏，作为独立的「关怀对象」板块，与既有政府监管 / 医疗机构 / 机构养老 / 社区养老 / 居家养老 并列。

## 数据来源与口径
- 沿用 `medical` 板块的「只读聚合」模式：新增 `elder` 板块从 `dataStore.elderly`（前端种子 + records API）**实时聚合**，按 `status === '在档'` 过滤。
- `buildElder()` + `watch(dataStore.elderly, {deep:true})`：后台改档后大屏即时刷新。
- 不进入 `all / SECTION_KEYS`，即**只读、不可后台手动编辑 / 重置**，与 medical 保持一致。

## 大屏呈现
- **导航**：顶部新增「关怀对象」页签（`#ffb020` 老人强调色，与青色主色区分）。
- **KPI**（3 卡）：在档关怀对象人数 / 失能·特护预警人数 / 平均年龄。
- **左栏图表**：各区县关怀对象分布（bar）、各区县照护等级构成（stack：自理 / 半失能 / 失能 / 特护）。
- **右栏图表**：照护等级构成（ring）、健康风险标签 TOP（hbar）、失能·特护预警率（gauge）。
- **地图**：`tab === 'elder'` 时叠加关怀对象散点（按区县中心花瓣定位，点击弹详情卡：区县 / 性别年龄 / 照护等级 / 居住类型 / 状态 / 健康标签 / 紧急联系人 / 所属机构）。
- 2D 与 3D 引擎点位、`allPoints` 面板列表均已纳入 elder。

## 改动文件
- `src/stores/sections.js`：新增 `elder` 只读聚合板块 + 导出。
- `src/views/BigScreen.vue`：nav / 图例 / CAT / makeDetail / `elderData` computed / 地图 series / allPoints / build3DMarkers / 图例 CSS。

## 校验
- `vite build` 通过（637 模块，0 错误）。
- 已重新部署至 CloudStudio 静态站点，链接见对话；种子数据内置前端 store，静态版亦可正常浏览关怀对象板块（无需后端）。

## 已知遗留（低优先级）
- 线上版仍为只读演示（无 Node 后端，增删改不落库）。
- 首屏 bundle 未做代码分割（gzip 后 JS ~493KB，构建 warning 持续）。
