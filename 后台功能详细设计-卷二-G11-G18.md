# 德阳智慧养老数字驾驶舱 · 后台管理系统　详细功能设计（卷二：G11–G18）

> 文档版本：v1.0　|　编制日期：2026-08-04　|　编制：UI Designer
> 配套文档：《后台功能差距分析.md》《后台功能详细设计-卷一-G01-G10.md》
> 交付层级：功能规格 + 核心交互流程 + 低保真线框原型（SVG 见主文档配套组件）
> 设计基线：沿用卷一首章「统一设计基线」（深色科技风 / a-card / a-btn / 1400·900 断点 / 状态色板）。

---

## 第三阶段 · 运营增强

### G11　财务与补贴管理　★资金流闭环

**优先级：中　|　业务价值：政府补贴是养老运营核心收入，需可追溯、可对账**

#### 1. 功能概述
服务费用结算、政府补贴发放/核销、对账记录、财务报表、发票管理。补贴对象为老人（G03），结算来源为工单（G06）。形成"工单→结算→补贴→对账"资金闭环。

#### 2. 角色与权限
| 角色 | 权限 |
|---|---|
| 财务管理员 | 结算/发放/对账/报表 |
| 区县级 | 本区数据 |
| 机构操作员 | 查看本机构账单 |

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `settlements` | `id` | string | ✅ | 结算单号 |
| | `orderId` | string | ✅ | 关联工单（G06） |
| | `elderId` | string | ✅ | 关联老人 |
| | `amount` | number | ✅ | 服务金额 |
| | `subsidy` | number | | 政府补贴额 |
| | `payee` | string | | 收款方（机构/专员） |
| | `status` | enum | ✅ | 待结算/已结算/已对账 |
| | `settledAt` | datetime | | 结算时间 |
| `subsidies` | `id`/`elderId`/`type`/`amount`/`period`/`status`/`issuedAt` | | ✅ | 补贴发放记录 |
| `reconciliations` | `id`/`period`/`totalAmount`/`diff`/`status` | | ✅ | 对账记录 |

#### 4. 核心业务流程（状态机）
```
工单完成(G06) ──► 生成待结算单 ──► [结算] ──► 已结算
                                        │
                    匹配补贴政策(elder.tags/abilityLevel) ──► 生成补贴发放
                                        │
                              周期对账 ──► 已对账(差异告警)
```

#### 5. 页面设计
| 页面 | 路由 | 布局 |
|---|---|---|
| 结算管理 | `/admin/finance/settle` | 列表（关联工单/老人/金额/状态）+ [结算] 批量；状态色标 |
| 补贴发放 | `/admin/finance/subsidy` | 按周期生成补贴清单（按能力等级/标签匹配政策），[发放] + 发放记录 |
| 对账中心 | `/admin/finance/recon` | 周期对账（系统额 vs 银行/财政回单），差异高亮 + 导出 |
| 财务报表 | 复用 G08 报表中心 | 收入/补贴/对账多维图表 |

**关键交互**：① 结算单与工单双向跳转；② 补贴自动匹配政策规则（可配置阈值）；③ 对账差异红标 + 一键导出差异明细。

---

### G12　评估管理

**优先级：中　|　业务价值：精准服务与补贴等级依据，失能等级直接决定服务方案与补贴额度**

#### 1. 功能概述
老人能力评估（ADL / 失能等级 / 认知评估）、评估周期管理、评估师资质、等级变更轨迹。评估对象为老人（G03），等级写回 `elders.abilityLevel`，评估报告存 G07。

#### 2. 角色与权限
评估师可新建评估；审核员可复核；管理员管理量表与周期。

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `assessments` | `id`/`elderId` | | ✅ | 关联老人 |
| | `scale` | enum | ✅ | 量表（ADL/MMSE/IADL…字典） |
| | `score` | number | ✅ | 得分 |
| | `level` | enum | ✅ | 等级（字典：能力评估等级） |
| | `assessor` | string | ✅ | 评估师 |
| | `status` | enum | ✅ | 草稿/待复核/已生效 |
| | `reportFile` | string | | 报告附件（G07） |
| | `assessedAt`/`nextAt` | datetime | | 评估/下次评估时间 |
| `assessScales` | `code`/`name`/`items[]`/`levels[]` | | ✅ | 量表定义（题项+等级映射） |

#### 4. 核心业务流程
```
新建评估 ──► 选量表+录项 ──► 算分 ──► 定级 ──► [提交复核] ──► 已生效 ──► 写回 elder.abilityLevel + 生成轨迹
                                                        │
                                              驳回 ──► 草稿(修改)
```

#### 5. 页面设计
`/admin/assess`：① 评估列表（老人/等级/状态/周期）；② 评估录入（量表动态表单 + 实时算分预览）；③ 量表管理（CRUD 题项与等级阈值）；④ 等级变更轨迹（老人维度时间线，对接 G03）。

---

### G13　消息中心 / 站内信

**优先级：中　|　业务价值：AlertCenter 已有通知"配置"但无独立收件箱，触达闭环未形成**

#### 1. 功能概述
站内信收件箱、已读/未读、消息模板、公告广播、@提醒。复用 AlertCenter 通知配置（G04 渠道）的发送能力，但提供用户侧接收与查阅界面。

#### 2. 角色与权限
所有登录用户有自己收件箱；管理员可发广播公告。

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `messages` | `id`/`title`/`content` | | ✅ | 消息 |
| | `type` | enum | ✅ | 系统/告警/工单/@/公告 |
| | `sender`/`receiver` | string | ✅ | 收发 |
| | `read` | bool | ✅ | 已读 |
| | `createdAt` | datetime | ✅ | 时间 |
| `msgTemplates` | `id`/`name`/`content` | | ✅ | 消息模板 |

#### 4. 核心业务流程
```
系统事件(告警/工单/审批) ──► 按 notifyConfig 渠道 ──► 站内信写入 messages(receiver)
                                                        │
用户登录 ──► 顶栏铃铛未读角标 ──► 收件箱 ──► 点击标记已读
管理员 ──► 发公告(选范围) ──► 批量写入
```

#### 5. 页面设计
顶栏铃铛（未读红角标）+ `/admin/messages` 收件箱（左分类：全部/未读/公告/告警/@ + 右列表，未读加粗 + 点开标记已读）；`模板`Tab（CRUD）；`发公告`Modal（范围选择 + 预览）。

---

## 第四阶段 · 运维完善

### G14　在线用户与会话管理

**优先级：低　|　业务价值：多账号安全管控，防止账号共享风险**

#### 1. 功能概述
查看在线用户列表、会话详情、强制下线、并发登录控制、异地登录提醒。依赖 G01 会话（sessions）。

#### 2. 数据模型
复用 `sessions` 实体（G01）+ 派生视图：用户/设备/IP/登录时间/时长/状态（活跃/闲置）。

#### 3. 核心业务流程
```
会话列表 ──► 选中 ──► [强制下线] ──► 吊销 token + 写日志
并发超限 ──► 踢出最早会话（策略可配）
异地/新设备 ──► 提醒(G13) + 标记
```

#### 4. 页面设计
`/admin/online-users`：表格（用户/组织/IP/设备/登录时长/状态）+ [强制下线]；支持按组织筛选；闲置超时策略配置（写 authConfig）。

---

### G15　菜单与按钮权限管理

**优先级：低　|　业务价值：当前菜单硬编码在 AdminLayout，权限模块仅有"模块勾选"无菜单/按钮粒度**

#### 1. 功能概述
可视化菜单树配置、按钮级权限标识、动态路由生成、角色菜单分配。将现有硬编码侧边栏升级为可配置。

#### 2. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `menus` | `id`/`parentId`/`name`/`path`/`icon`/`sort`/`visible`/`perms` | | ✅ | 菜单（含按钮标识） |
| `roleMenus` | `roleId`/`menuIds[]` | | ✅ | 角色菜单映射 |

#### 3. 核心业务流程
```
菜单树(CRUD) ──► 角色分配菜单 ──► 前端动态路由(按 roleMenus 过滤 AdminLayout) + 按钮 v-if(perms)
```

#### 4. 页面设计
`/admin/menu`：左菜单树（拖拽排序 + 显隐开关）+ 右表单（名称/路由/图标/父级/按钮权限标识）；`角色菜单`Tab（树形勾选分配，复用现有 roles）。

---

### G16　系统参数配置

**优先级：低　|　业务价值：运维灵活配置无需改代码，当前仅可视化参数可配**

#### 1. 功能概述
通用系统参数：分页大小、会话超时、水印开关、敏感词、导出限制、环境切换。键值对存储，全局生效。

#### 2. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `sysParams` | `key`/`value`/`group`/`remark`/`type` | | ✅ | 参数键值 |

#### 3. 页面设计
`/admin/sys-params`：分组卡片（基础/安全/导出/显示）+ 键值编辑（输入/开关/下拉按 type）；保存即生效（前端 reload 或监听）。

---

### G17　缓存与服务器监控增强

**优先级：低　|　业务价值：Monitor 已有应用指标，缺基础设施层监控**

#### 1. 功能概述
Redis/内存缓存命中率监控、服务器 CPU/内存/磁盘/JVM、网络流量。在现有 Monitor 模块扩展。

#### 2. 数据模型
`metrics` 扩展维度：`cacheHitRate`/`cpu`/`mem`/`disk`/`netIn`/`netOut`（后端采集，前端 `useChart` 时序图）。

#### 3. 页面设计
在 `/admin/monitor` 增「基础设施」Tab：服务器卡片（CPU/内存/磁盘进度环）+ 缓存命中率趋势 + 网络流量双轴图；阈值超标红标 + 联动告警（G04）。

---

### G18　数据备份与恢复

**优先级：低（但建议尽早）　|　业务价值：数据安全兜底，cockpit.json 单点存储无备份策略，存在丢失风险**

#### 1. 功能概述
自动备份策略（全量/增量）、备份历史、一键恢复、异地容灾。保护 `server/data/cockpit.json`。

#### 2. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `backups` | `id`/`type`(全量/增量)/`size`/`path`/`createdAt`/`status` | | ✅ | 备份记录 |
| `backupPolicy` | `auto`/`cron`/`retain`/`remote` | | ✅ | 备份策略 |

#### 3. 核心业务流程
```
手动[立即备份] ──► 快照 cockpit.json ──► 存 backups(压缩)
定时(G05) 自动备份 ──► 按 retain 清理过期
[恢复] ──► 选备份 ──► 校验 ──► 覆盖 cockpit.json + 重启服务(提示)
```

#### 4. 页面设计
`/admin/backup`：① 策略配置（自动开关/Cron/保留份数/异地）；② 备份历史列表（类型/大小/时间/状态）+ [恢复][下载]；③ 恢复前二次确认 + 自动先打当前快照（防误操作）。

---

## 附：四阶段 SVG 线框原型索引

以下核心模块配低保真 SVG 线框，详见主文档《后台功能详细设计.md》内嵌组件：
- **G01 登录页** — 居中卡片布局
- **G03 老人档案详情** — Tab 分段 + 信息层级
- **G06 工单调度看板** — 状态列拖拽看板
- **G04 数据字典** — 左树右表主从
- **G05 定时任务** — 列表 + 启停 + 日志抽屉
- **G11 财务结算** — 结算单 + 补贴匹配

---

*卷二结束（G11–G18）。全 18 项详细设计交付完成。*
