# G04 数据字典（德阳市智慧养老数字驾驶舱）

> **文档类型**：数据库 / 数据模型设计说明书（开发对接基线）
> **适用系统**：德阳市智慧养老数字驾驶舱 — 后台管理 + 驾驶舱大屏
> **数据层技术**：前端 `records API` + Node JSON 文件后端（`server/data/cockpit.json`），按实体名分表存储；字段命名统一 **camelCase**。
> **编写**：UI Designer ｜ **日期**：2026-08-04
> **数据状态**：本字典以 `src/stores/data.js` 中真实 seed 结构与 CRUD 行为为准，新增/调整字段以此文档为权威来源。

---

## 1. 全局约定

### 1.1 存储模型说明（重要）
- 后端为 **JSON 文件库**，每个「实体（entity）」对应 `cockpit.json` 中的一个数组（或单例对象），**不存在数据库级外键约束**。
- 表与表之间的关系均为 **语义 / 逻辑关联**（通过 `id` 或 `name` 字段匹配），非物理外键。建议在后续版本引入显式关联键（见第 5 节建议）。
- 主键统一为字符串 `id`，由前端 `uid(prefix)` 生成（前缀见下表），持久化时后端按 `id` 做 upsert / 删除。
- 单例配置类实体（`notifyConfig`、`authConfig`）以固定 `id = 'CFG'` 存储为数组中的唯一对象。

### 1.2 字段类型图例
| 类型 | 含义 |
|------|------|
| String | 字符串 |
| Number | 数值（整型 / 浮点） |
| Boolean | 布尔（true / false） |
| Object | 嵌套 JSON 对象 |
| Array | 数组 |
| DateStr | 时间字符串，统一格式 `YYYY-MM-DD HH:mm`（配置类用 `YYYY-MM-DD`） |

### 1.3 实体清单与主键前缀
| 实体名（后端表） | 中文名 | 主键前缀 | 形态 | 说明 |
|------------------|--------|----------|------|------|
| institutions | 养老机构 | JG | 数组 | 政府监管 + 机构养老共用 |
| facilities | 社区养老设施 | SQ | 数组 | 长者食堂 / 日间照料等 |
| orders | 居家养老工单 | O | 数组 | 助餐 / 助洁 / 护理等 |
| users | 系统用户 | U | 数组 | 后台登录账号（RBAC 主体） |
| roles | 角色 | R | 数组 | RBAC 权限矩阵 |
| contents | 内容运营 | C | 数组 | 科普 / 公告 / 视频等 |
| sources | 数据源 | S | 数组 | 第三方接入源 |
| devices | 物联设备 | D | 数组 | 烟感 / SOS / 血压计等 |
| alerts | 告警记录 | A | 数组 | 安全 / 健康 / 设备告警 |
| alertRules | 告警规则 | R | 数组 | 阈值监控规则 |
| metrics | 监控指标 | M | 数组 | 运维监控指标 |
| loginLogs | 登录日志 | LL | 数组 | 认证审计 |
| medical | 医疗机构 | MED | 数组 | 卫生专网签约机构（预留，当前为空） |
| notifyConfig | 通知配置 | CFG | 单例对象 | 渠道 / 静默 / 升级 |
| authConfig | 安全策略配置 | CFG | 单例对象 | 登录锁定 / 验证码策略 |
| logs | 操作日志 | LOG | 数组（仅前端） | 本地，不落库 |

> ⚠️ 注意：`roles` 与 `alertRules` 的主键前缀均为 `R`，二者属不同实体，不会冲突；但建议在字典层区分（角色 `R00x`、规则 `R00x`）以免人肉混淆，文档中已用「角色」「规则」语境区分。

---

## 2. 各数据表字段定义

### 2.1 institutions（养老机构）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 机构主键，如 `JG001` |
| code | String | 12 | 否 | = id | — | 机构编码，唯一业务标识 |
| name | String | 60 | 否 | — | — | 机构全称 |
| district | String | 20 | 否 | — | 逻辑关联 | 标准区县（旌阳区/罗江区/中江县/广汉市/什邡市/绵竹市） |
| type | String | 20 | 否 | — | 枚举 | 机构类型：公办养老 / 民办非企 / 社区嵌入式 |
| beds | Number | — | 否 | 0 | — | 床位总数 |
| status | String | 12 | 否 | 运营中 | 枚举 | 运营状态：运营中 / 整改中 / 筹建中 |
| elders | Number | — | 否 | 0 | — | 在院老人数 |
| occupancy | String | 8 | 否 | 0% | — | 入住率，百分比字符串，如 `93%` |
| assess | String | 20 | 是 | — | 枚举 | 照护等级分布：混合 / 自理偏多 / 失能偏多 |

### 2.2 facilities（社区养老设施）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 设施主键，如 `SQ001` |
| code | String | 12 | 否 | = id | — | 设施编码 |
| name | String | 60 | 否 | — | — | 设施名称 |
| community | String | 30 | 否 | — | — | 所属社区，如 `旌阳区沱江` |
| type | String | 20 | 否 | — | 枚举 | 设施类型：长者食堂 / 日间照料 / 助老服务 / 文娱活动 / 康复护理 |
| monthly | Number | — | 否 | 0 | — | 月服务人次 |
| status | String | 12 | 否 | 运营中 | 枚举 | 运营状态：运营中 |

### 2.3 orders（居家养老工单）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 工单主键，如 `O001` |
| code | String | 20 | 否 | 自动生成 | — | 工单编号，如 `GD202607001` |
| elder | String | 40 | 否 | — | — | 服务对象描述，如 `张某某（独居）` |
| district | String | 20 | 否 | — | 逻辑关联 | 服务区县（同 institutions.district 词表） |
| service | String | 12 | 否 | — | 枚举 | 服务类型：助餐 / 助洁 / 护理 / 助浴 / 陪伴 |
| response | String | 12 | 否 | — | — | 响应时长，带单位字符串，如 `7.2 分钟` |
| status | String | 12 | 否 | 待派单 | 枚举 | 工单状态：已完成 / 进行中（预留：待派单 / 已取消） |

### 2.4 users（系统用户）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 用户主键，如 `U001` |
| acc | String | 30 | 否 | — | — | 登录账号，唯一 |
| name | String | 30 | 否 | — | — | 用户姓名 / 称呼 |
| role | String | 20 | 否 | — | **逻辑FK→roles.name** | 角色名称（建议改造为 `roleId` 关联 roles.id） |
| org | String | 60 | 是 | — | — | 所属组织，如 `德阳市民政局` |
| phone | String | 20 | 是 | — | — | 手机号，脱敏存储，如 `138****0001` |
| on | Boolean | — | 否 | true | — | 是否启用（false = 禁用，登录拒绝） |
| password | String | 64 | 否 | 123456 | — | 登录密码（DEMO 明文；生产须哈希加盐） |

### 2.5 roles（角色 · RBAC）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 角色主键，如 `R001` |
| name | String | 20 | 否 | — | — | 角色名：超级管理员 / 市监管账号 / 区县账号 / 机构管理员 |
| scope | String | 12 | 否 | 本机构 | 枚举 | 数据范围：全市 / 本区县 / 本机构 |
| users | Number | — | 否 | 0 | — | 关联用户数（冗余统计，非外键） |
| perms | Object | — | 否 | 见说明 | — | 权限矩阵：`{ 总览/业务数据/数据接入/告警/物联设备/可视化配置/权限/内容/审计/监控: Boolean }` |

### 2.6 contents（内容运营）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 内容主键，如 `C001` |
| title | String | 80 | 否 | — | — | 内容标题 |
| category | String | 20 | 否 | — | 枚举 | 分类：科普文章 / 政策解读 / 通知公告 / 宣教视频 / 活动招募 |
| status | String | 12 | 否 | 草稿 | 枚举 | 发布状态：已发布 / 草稿 / 已下线 |
| publishAt | DateStr(YYYY-MM-DD) | 10 | 是 | '' | — | 发布日期；已发布时自动写入当天 |

### 2.7 sources（数据源）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 数据源主键，如 `S001` |
| name | String | 60 | 否 | — | — | 数据源名称 |
| type | String | 20 | 否 | — | 枚举 | 类型：省级接口 / 本地数据库 / 市级接口 / 设备接入 / 呼叫中心 |
| status | String | 12 | 否 | 正常 | 枚举 | 同步状态：正常 / 警告 |
| lastSync | DateStr | 16 | 是 | — | — | 最近同步时间 `YYYY-MM-DD HH:mm` |
| interval | String | 12 | 否 | — | 枚举 | 同步频率：实时 / 每5分钟 / 每15分钟 / 每30分钟 / 每小时 |

### 2.8 devices（物联设备）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 设备主键，如 `D001` |
| code | String | 12 | 否 | = id | — | 设备编码 |
| name | String | 60 | 否 | — | — | 设备名称，含区域编号 |
| type | String | 20 | 否 | — | 枚举 | 类型：烟感探测器 / SOS呼叫 / 红外感应 / 健康检测 / 燃气报警 / 睡眠监测 |
| area | String | 30 | 否 | — | 逻辑关联 | 部署区域（语义关联 alerts.area / institutions.district） |
| status | String | 12 | 否 | 在线 | 枚举 | 在线状态：在线 / 离线 / 故障 |
| last | DateStr | 16 | 是 | — | — | 最近上报时间 `YYYY-MM-DD HH:mm` |

### 2.9 alerts（告警记录）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 告警主键，如 `A001` |
| level | String | 8 | 否 | — | 枚举 | 等级：紧急 / 严重 / 一般 / 提示 |
| title | String | 80 | 否 | — | — | 告警标题 |
| type | String | 16 | 否 | — | 枚举 | 类型：安全事件 / 健康预警 / 设备异常 / 服务异常 |
| source | String | 16 | 否 | — | 枚举 | 来源：物联设备 / 系统监测 / 人工上报 / 第三方接口 |
| area | String | 30 | 否 | — | 逻辑关联 | 详细区域（语义关联 devices.area） |
| district | String | 20 | 否 | — | 逻辑关联 | 标准区县（同 institutions.district 词表） |
| ruleName | String | 30 | 否 | — | **逻辑FK→alertRules.name** | 触发规则名 |
| time | DateStr | 16 | 否 | — | — | 发生时间 `YYYY-MM-DD HH:mm`（可排序） |
| state | String | 12 | 否 | 待处理 | 枚举 | 处置状态：待处理 / 处理中 / 已解决 / 已忽略 |
| handler | String | 30 | 是 | '' | — | 处置人 |
| note | String | 200 | 是 | '' | — | 处置说明 |
| resolvedAt | DateStr | 16 | 是 | '' | — | 解决时间；state=已解决时自动写入 |

### 2.10 alertRules（告警规则）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 规则主键，如 `R001` |
| name | String | 40 | 否 | — | — | 规则名称 |
| target | String | 40 | 否 | — | **逻辑FK→metrics.name** | 监控指标名 |
| op | String | 2 | 否 | > | 枚举 | 比较运算符：> / < |
| threshold | Number | — | 否 | 0 | — | 触发阈值（数值） |
| unit | String | 6 | 否 | — | 枚举 | 单位：% / ms / 次 / 个 |
| level | String | 8 | 否 | 一般 | 枚举 | 触发后告警等级：紧急 / 严重 / 一般 / 提示 |
| channel | String | 20 | 否 | 站内 | 枚举 | 通知渠道：站内 / 短信+站内 / 电话+短信 |
| silence | Number | — | 否 | 0 | — | 静默期（分钟），0 = 不静默 |
| enabled | Boolean | — | 否 | true | — | 是否启用 |

### 2.11 metrics（监控指标）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 指标主键，如 `M001` |
| name | String | 40 | 否 | — | — | 指标名（被 alertRules.target 引用） |
| value | String | 16 | 否 | — | — | 指标值，含数值或带单位字符串（如 `38%` / `126`） |
| unit | String | 6 | 否 | '' | 枚举 | 单位：% / ms / 次 / 个 / '' |
| status | String | 8 | 否 | 健康 | 枚举 | 健康状态：健康 |
| trend | String | 8 | 否 | 平稳 | 枚举 | 趋势：平稳 / 上升（预留：下降） |

### 2.12 loginLogs（登录日志）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 日志主键，如 `LL001` / `LLn` |
| userId | String | 12 | 否 | — | **逻辑FK→users.id** | 关联用户 |
| username | String | 30 | 否 | — | — | 登录账号（冗余，便于审计检索） |
| ip | String | 20 | 否 | — | — | 登录 IP |
| device | String | 60 | 否 | — | — | 设备 / 浏览器 UA 摘要 |
| location | String | 20 | 否 | 德阳市 | — | 地理位置 |
| result | String | 8 | 否 | — | 枚举 | 结果：success / fail / locked |
| failReason | String | 40 | 是 | — | — | 失败原因（result≠success 时填写） |
| time | DateStr | 16 | 否 | — | — | 登录时间 `YYYY-MM-DD HH:mm` |

### 2.13 medical（医疗机构 · 预留）
> 实体已注册（`medCrud`，前缀 `MED`，持久化实体 `medical`），但当前无任何 seed 数据。以下为**建议 Schema**，待 G09 健康监测 / 卫生专网接入时落地。

| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 12 | 否 | — | **PK** | 机构主键，如 `MED001` |
| code | String | 12 | 否 | = id | — | 机构编码 |
| name | String | 60 | 否 | — | — | 机构全称 |
| type | String | 20 | 否 | — | 枚举 | 类型：综合医院 / 社区卫生服务中心 / 乡镇卫生院 / 专科医院 |
| district | String | 20 | 否 | — | 逻辑关联 | 标准区县 |
| level | String | 12 | 否 | — | 枚举 | 医院等级：三甲 / 三乙 / 二甲 / 一级 |
| beds | Number | — | 否 | 0 | — | 床位总数 |
| doctors | Number | — | 否 | 0 | — | 执业医师数 |
| contract | Boolean | — | 否 | false | — | 是否签约（卫生专网） |
| phone | String | 20 | 是 | — | — | 联系电话 |

### 2.14 notifyConfig（通知配置 · 单例）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 8 | 否 | CFG | **PK** | 固定 `CFG`（单例） |
| channels | Object | — | 否 | 见下 | — | 渠道配置，含 site / email / sms 三个子对象 |
| channels.site | Object | — | 否 | — | — | 站内信：`{ enabled:Boolean, recipients:String, levels:Array<String> }` |
| channels.email | Object | — | 否 | — | — | 邮件：`{ enabled:Boolean, address:String, levels:Array<String> }` |
| channels.sms | Object | — | 否 | — | — | 短信：`{ enabled:Boolean, phones:String, levels:Array<String> }` |
| quietHours | Object | — | 否 | — | — | 免打扰：`{ enabled:Boolean, start:String(HH:mm), end:String(HH:mm) }` |
| escalation | Object | — | 否 | — | — | 升级：`{ enabled:Boolean, minutes:Number, channel:String }` |

> `levels` 取值复用告警等级枚举：紧急 / 严重 / 一般 / 提示。

### 2.15 authConfig（安全策略配置 · 单例）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 8 | 否 | CFG | **PK** | 固定 `CFG`（单例） |
| maxFail | Number | — | 否 | 5 | — | 连续失败锁定阈值（次） |
| lockMinutes | Number | — | 否 | 30 | — | 账号锁定时长（分钟） |
| captchaEnabled | Boolean | — | 否 | true | — | 图形验证码总开关 |
| captchaThreshold | Number | — | 否 | 2 | — | 失败达此次数后出现验证码 |
| 异地提醒 | Boolean | — | 否 | false | — | 异地 / 新设备登录提醒开关 |

### 2.16 logs（操作日志 · 仅前端）
| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 16 | 否 | — | **PK** | 日志主键，如 `LOG1001` |
| time | DateStr | 16 | 否 | — | — | 操作时间 `YYYY-MM-DD HH:mm` |
| action | String | 20 | 否 | — | — | 操作类型，如 新增机构 / 处置告警 |
| target | String | 60 | 否 | — | — | 操作对象描述 |
| user | String | 30 | 否 | 当前管理员 | — | 操作人 |

> 该实体 `entity` 参数为空，仅前端 `reactive` 内存，刷新即丢失，**不落库**；如需持久化审计请改用 `loginLogs` 同款落库机制。

---

### 2.17 elderly（老人档案 · G03 关怀对象主数据）

> 主键前缀 `E`；`entity='elderly'`，经 records API **落库**；为居家 / 社区 / 机构养老三类场景的关怀对象主数据，与 `institutions` 通过机构名逻辑关联。

| 字段名 | 类型 | 长度 | 可空 | 默认值 | 主键/外键 | 字段说明 |
|--------|------|------|------|--------|-----------|----------|
| id | String | 16 | 否 | — | **PK** | 档案主键，如 `E001` |
| code | String | 16 | 否 | =id | 业务号 | 档案号（前端展示用） |
| name | String | 30 | 否 | — | — | 老人姓名 |
| gender | Enum | 2 | 否 | 男 | — | 性别（见 §4 性别） |
| age | Number | 3 | 否 | — | — | 年龄（1–120） |
| idNo | String | 18 | 是 | '' | — | 身份证号（可掩码；DEMO 明文，生产须哈希） |
| district | Enum | 12 | 否 | — | 逻辑→institutions | 标准区县（见 §4 区县） |
| town | String | 30 | 是 | '' | — | 街道 / 乡镇 |
| address | String | 80 | 是 | '' | — | 详细住址 |
| phone | String | 20 | 是 | '' | — | 联系电话（11 位或掩码） |
| livingType | Enum | 12 | 否 | 独居 | — | 居住类型（见 §4 居住类型） |
| careLevel | Enum | 8 | 否 | 自理 | — | 照护等级（见 §4 照护等级·老人） |
| status | Enum | 8 | 否 | 在档 | — | 档案状态（见 §4 档案状态） |
| belongOrg | String | 40 | 是 | '' | 逻辑→institutions.name | 所属机构 / 服务商名称 |
| emergencyName | String | 30 | 是 | '' | — | 紧急联系人姓名 |
| emergencyPhone | String | 20 | 是 | '' | — | 紧急联系人电话 |
| emergencyRelation | Enum | 12 | 否 | 子女 | — | 与本人关系（见 §4 与本人关系） |
| healthTags | Array\<String\> | — | 是 | [] | — | 健康标签数组（见 §4 健康标签） |
| registerDate | DateStr | 10 | 是 | '' | — | 建档日期 `YYYY-MM-DD` |

---

## 3. 表间关联关系

| 关系 | 类型 | 关联字段 | 说明 |
|------|------|----------|------|
| users → roles | 逻辑（按名） | `users.role` = `roles.name` | 用户归属角色；建议改造为 `roleId` = `roles.id` |
| alerts → alertRules | 逻辑（按名） | `alerts.ruleName` = `alertRules.name` | 告警反查触发规则；建议改造为 `ruleId` = `alertRules.id` |
| alertRules → metrics | 逻辑（按名） | `alertRules.target` = `metrics.name` | 规则绑定监控指标 |
| loginLogs → users | 逻辑（按ID） | `loginLogs.userId` = `users.id` | 登录日志归属用户（已是 ID 关联，规范） |
| institutions / orders / alerts / devices / medical | 语义（按区县/区域） | `*.district` / `*.area` | 通过标准区县词表或区域字符串横向关联，非硬外键 |
| elderly → institutions | 逻辑（按名） | `elderly.belongOrg` = `institutions.name` | 老人归属服务机构的逻辑关联；建议改造为 `orgId` = `institutions.id` |

无显式外键约束，删除主表记录时**不会**级联清理从表；业务层需自行保证一致性（如停用角色前先迁移用户）。

---

## 4. 受控词表（枚举字典 · 全系统统一）

> 「枚举优先走 G04 字典」——以下取值在新增/编辑表单中应以下拉或标签形式受控，避免自由文本导致统计失真。

| 维度 | 取值集合 |
|------|----------|
| 区县 district | 旌阳区 / 罗江区 / 中江县 / 广汉市 / 什邡市 / 绵竹市 |
| 机构类型 | 公办养老 / 民办非企 / 社区嵌入式 |
| 运营状态 | 运营中 / 整改中 / 筹建中 |
| 照护等级分布 | 混合 / 自理偏多 / 失能偏多 |
| 设施类型 | 长者食堂 / 日间照料 / 助老服务 / 文娱活动 / 康复护理 |
| 服务类型 | 助餐 / 助洁 / 护理 / 助浴 / 陪伴 |
| 工单状态 | 已完成 / 进行中（预留：待派单 / 已取消） |
| 角色名 | 超级管理员 / 市监管账号 / 区县账号 / 机构管理员 |
| 数据范围 | 全市 / 本区县 / 本机构 |
| 内容分类 | 科普文章 / 政策解读 / 通知公告 / 宣教视频 / 活动招募 |
| 内容状态 | 已发布 / 草稿 / 已下线 |
| 数据源类型 | 省级接口 / 本地数据库 / 市级接口 / 设备接入 / 呼叫中心 |
| 同步状态 | 正常 / 警告 |
| 同步频率 | 实时 / 每5分钟 / 每15分钟 / 每30分钟 / 每小时 |
| 设备类型 | 烟感探测器 / SOS呼叫 / 红外感应 / 健康检测 / 燃气报警 / 睡眠监测 |
| 设备状态 | 在线 / 离线 / 故障 |
| 告警等级 | 紧急 / 严重 / 一般 / 提示 |
| 告警类型 | 安全事件 / 健康预警 / 设备异常 / 服务异常 |
| 告警来源 | 物联设备 / 系统监测 / 人工上报 / 第三方接口 |
| 告警处置状态 | 待处理 / 处理中 / 已解决 / 已忽略 |
| 比较运算符 | > / < |
| 指标单位 | % / ms / 次 / 个 / ''（无） |
| 指标趋势 | 平稳 / 上升（预留：下降） |
| 通知渠道 | 站内 / 短信+站内 / 电话+短信 / 企业微信 |
| 登录结果 | success / fail / locked |
| 医院等级（medical·预留） | 三甲 / 三乙 / 二甲 / 一级 |
| 性别 gender | 男 / 女 |
| 居住类型 | 独居 / 空巢 / 与子女同住 / 机构养老 / 社区养老 |
| 照护等级（老人） | 自理 / 半失能 / 失能 / 特护 |
| 档案状态 | 在档 / 离档 / 转院 |
| 与本人关系 | 配偶 / 子女 / 父母 / 亲属 / 邻居 / 社区网格员 / 其他 |
| 健康标签 | 高血压 / 糖尿病 / 冠心病 / 脑卒中后遗症 / 认知障碍 / 骨质疏松 / 褥疮风险 / 视听障碍 |

---

## 5. 设计建议（落地校验 & 后续优化）

1. **显式外键**：将 `users.role`、`alerts.ruleName` 改为 `roleId` / `ruleId` 直接关联 `roles.id` / `alertRules.id`，消除「按名匹配」带来的重命名风险。
2. **密码安全**：`users.password` 当前明文存储（DEMO），生产环境须改为哈希加盐（bcrypt / argon2），前端不回显。
3. **medical 实体**：尽快落地第 2.13 节建议 Schema，并在大屏独立图层接入前完成字段确认。
4. **logs 持久化**：若需审计留痕，将操作日志纳入 records API 落库（参考 `loginLogs` 的 `logLogin` 模式）。
5. **枚举中心化**：已落地为 `src/constants/dict.js`（含老人档案 6 组枚举），表单组件统一引用，保证前后端取值一致。新增枚举请先在此文档登记，再登记到 dict.js。

---

**UI Designer** ｜ 数据字典作为开发对接基线，字段定义以 `src/stores/data.js` 当前实现为准。新增任何业务实体请先在此文档登记字段，再落地代码。
