# 德阳智慧养老数字驾驶舱 · 后台管理系统　详细功能设计（卷一：G01–G10）

> 文档版本：v1.0　|　编制日期：2026-08-04　|　编制：UI Designer
> 配套文档：《后台功能差距分析.md》《后台功能详细设计-卷二-G11-G18.md》
> 交付层级：功能规格 + 核心交互流程 + 低保真线框原型（SVG 见主文档配套组件）

---

## 〇、统一设计基线（全卷通用）

详细设计的所有界面均复用现有后台已建立的设计语言，确保新增模块与已实现的 11 个模块视觉与交互一致，不产生设计债。

### 0.1 视觉与组件规范
- **风格**：深色科技风（背景 `#0a1220` / 面板 `#0f1b30` / 边框 `rgba(255,255,255,.08)`），主色青 `#00f0ff`、辅色蓝 `#3b82f6`、告警橙 `#ffaa00`、危险红 `#ff4d4d`。
- **基础组件**（已实现，直接复用）：`a-card`（卡片容器）、`a-btn`（按钮，含 primary/ghost/danger 变体）、`Modal`（表单弹层）、`DetailDrawer`（右侧详情抽屉）、`useTable`（筛选/排序/分页/批量）、`useChart`（ECharts 封装）、`useToast`（全局提示）、`exportCsv`（导出）、`IconSvg`（图标库）。
- **栅格与响应式**：沿用容器断点 `1400px / 900px`；KPI 卡与图表网格在 <1400 折为 2 列、<900 折为 1 列；表格在 <900 启用横向滚动并简化列。
- **表单规范**：标签左对齐 `label-width: 96px`；必填项 `*` 红标；输入框聚焦青色描边 + 浅光晕；错误态红框 + 行内提示；提交按钮统一右下角。
- **状态色板（全局统一）**：
  | 语义 | 颜色 | 用途 |
  |---|---|---|
  | 紧急 | `#ff4d4d` | 告警紧急级 |
  | 严重 | `#ffaa00` | 告警严重级 |
  | 一般 | `#ffd400` | 告警一般级 |
  | 提示 | `#3b82f6` | 告警提示级 |
  | 成功 | `#10b981` | 已解决/正常 |
  | 信息 | `#00f0ff` | 进行中/待处理 |

### 0.2 数据持久化约定
- 所有新增实体沿用 `records` API（`PUT /api/records/:entity` 整表替换 + `GET` 读取），落盘 `server/data/cockpit.json`。
- 字段命名 `camelCase`；时间统一 ISO 字符串；枚举值尽量走 G04 数据字典，不硬编码。
- 列表页统一支持：搜索、多选（checkbox 列）、排序（表头点击）、分页（每页 10/20/50）、批量操作条、导出 CSV、详情抽屉。

### 0.3 通用页面骨架（列表型模块）
```
┌─────────────────────────────────────────────┐
│ 页面标题             [新建] [批量操作▼] [导出] │  ← 顶部操作区
├─────────────────────────────────────────────┤
│ [搜索框] [筛选①▼] [筛选②▼] [状态▼] [重置]    │  ← 工具栏
├─────────────────────────────────────────────┤
│ ▢ 列1  列2  列3  …  状态      操作            │  ← 表头(可排序)
│ ▢ …                                         │
│  （空态：图标 + "暂无数据" + 引导操作）        │
├─────────────────────────────────────────────┤
│ 共 N 条   < 1 2 3 … >   每页[20▼]            │  ← 分页
└─────────────────────────────────────────────┘
```

---

## 第一阶段 · 安全基线与核心数据

### G01　登录与认证中心

**优先级：高　|　业务价值：安全基线，当前系统无任何身份校验，最高风险缺口**

#### 1. 功能概述
为后台提供统一的身份认证入口与访问守卫。当前后台访问 `/admin` 即可直接操作全部数据与配置，顶栏管理员身份写死，属安全隐患最高优先级。本模块建成后：所有后台路由受 `beforeEach` 守卫保护，未登录跳转登录页；登录失败达阈值自动锁定；支持账号密码 + 图形验证码 + 记住我 + SSO（政务微信/钉钉）对接；会话采用 JWT（accessToken + refreshToken）。

#### 2. 角色与权限
| 角色 | 说明 | 登录权限 |
|---|---|---|
| 超级管理员 | 全量权限 | ✅ |
| 区县级管理员 | 本区县数据 | ✅ |
| 机构操作员 | 本机构网点/工单 | ✅ |
| 访客/未登录 | — | ❌（守卫拦截） |

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `users`（扩展） | `id` | string | ✅ | 账号 |
| | `passwordHash` | string | ✅ | bcrypt 加密，不存明文 |
| | `salt` | string | ✅ | 加密盐 |
| | `avatar` | string | | 头像 URL |
| | `loginFailCount` | int | ✅ | 连续失败计数 |
| | `lockUntil` | datetime | | 锁定到期时间，空=未锁 |
| | `lastLoginAt` | datetime | | 最近登录 |
| | `lastLoginIp` | string | | 最近登录 IP |
| | `status` | enum | ✅ | 启用/禁用（已有） |
| `sessions` | `token` | string | ✅ | JWT accessToken |
| | `refreshToken` | string | ✅ | 刷新令牌 |
| | `userId` | string | ✅ | 关联用户 |
| | `expiresAt` | datetime | ✅ | 过期时间 |
| | `device`/`ip` | string | | 登录设备/IP |
| `authConfig` | `captchaEnabled` | bool | ✅ | 是否启用验证码 |
| | `maxFail` | int | ✅ | 锁定阈值，默认 5 |
| | `lockMinutes` | int | ✅ | 锁定时长，默认 30 |
| | `ssoEnabled` | bool | ✅ | SSO 开关 |
| | `ssoType` | enum | | wecom/dingsync |

#### 4. 核心业务流程（状态机）
```
                  ┌─────────────┐
   访问/admin ──► │ 守卫拦截?   │── 已登录 ──► 进入目标页
                  │ 未登录      │
                  └──────┬──────┘
                         ▼
                  ┌─────────────┐
                  │ 登录页      │  账号+密码+验证码
                  └──────┬──────┘
                         ▼
                  ┌─────────────┐   失败
         校验 ──► │ 失败计数+1  │──────────┐
                  └──────┬──────┘          │
                   成功  │           达阈值 │
                         ▼                 ▼
                  ┌─────────────┐   ┌─────────────┐
                  │ 签发 JWT     │   │ 账号锁定     │
                  │ 写入会话     │   │ lockUntil    │
                  └──────┬──────┘   └─────────────┘
                         ▼
                  进入后台首页（重置失败计数）
```
- **锁定策略**：连续失败 ≥ `maxFail` 次 → `lockUntil = now + lockMinutes`；锁定期间登录直接拒绝并提示剩余时间。
- **过期刷新**：accessToken 过期 → 用 refreshToken 静默换发；refreshToken 过期 → 强制重新登录。
- **登出**：清除本地 token + 后端吊销 session + 写登录日志（G10）。

#### 5. 页面设计
| 页面 | 路由 | 布局要点 |
|---|---|---|
| 登录页 | `/login` | 居中卡片：系统 Logo + 标题；账号/密码输入；图形验证码（失败≥2 次出现）；记住我 + 忘记密码；SSO 入口按钮（启用时）；底部版权。错误抖动 + 红框提示。锁定态显示倒计时。 |
| 锁定提示页 | `/login?locked=1` | 红框提示"账号已锁定，剩余 X 分钟"，禁止输入。 |
| SSO 回调 | `/sso/callback` | 透明中转页，接收 code 换 token，成功跳首页。 |
| 403/无权限 | `/403` | 提示无权限访问该模块。 |

**关键交互**：① 首次失败不弹验证码，第 2 次起出现；② 密码框右侧眼睛图标切换明文；③ 回车提交；④ 登录成功写 `loginLogs`（G10 联动）；⑤ 守卫对白名单路由（`/login`）放行。

---

### G02　个人中心

**优先级：高　|　业务价值：账号自助管理，降低运维成本，认证体系必备配套**

#### 1. 功能概述
登录用户维护自身资料、修改密码、查看最近登录记录与安全设置。入口为顶栏头像下拉「个人中心」。

#### 2. 角色与权限
所有已登录角色均可访问自身中心，仅能改自己数据。

#### 3. 数据模型
复用 `users` + `loginLogs`（G10 提供记录）。本模块新增本地表单态：`oldPassword`/`newPassword`/`confirmPassword`；`twoFactorEnabled`（二次验证开关）。

#### 4. 核心业务流程
```
个人中心 ──┬─ 资料卡：展示头像/账号/角色/所属组织/状态 ── 编辑资料(昵称/头像/手机号)
           ├─ 安全设置：修改密码(旧→新→确认，强度校验)
           └─ 登录记录：最近 N 条(时间/IP/设备/结果)，异常标红
```
- **改密校验**：旧密码正确 + 新密码强度（≥8 位含大小写数字）+ 两次一致 → 调 `updateUser` 重设 `passwordHash`，登出重登。

#### 5. 页面设计
顶栏头像下拉 → 抽屉式或独立页 `/admin/profile`，三段式 Tab（资料 / 安全 / 登录记录），复用 `a-card` + 表单规范。

---

### G03　老人档案管理　★核心业务实体

**优先级：高　|　业务价值：养老平台最核心数据对象，当前仅有"网点"无"被服务对象"，大屏人口数据无据可依**

#### 1. 功能概述
建立全市老年人档案主数据，涵盖基本信息、健康档案、家属联系人、服务记录、能力评估等级。作为驾驶舱人口统计、工单（G06）、健康监测（G09）、评估（G12）、财务补贴（G11）的统一数据源。

#### 2. 角色与权限
| 角色 | 权限 |
|---|---|
| 超级管理员 | 全市档案 CRUD |
| 区县级 | 本区县 CRUD |
| 机构操作员 | 本机构老人只读 + 服务记录追加 |
| 访客 | 无 |

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `elders` | `id` | string | ✅ | 档案号（EL+区县码+序号） |
| | `name` | string | ✅ | 姓名 |
| | `idCard` | string | ✅ | 身份证（加密存储） |
| | `gender` | enum | ✅ | 男/女（字典 G04） |
| | `birth` | date | ✅ | 出生日期（算年龄） |
| | `phone` | string | | 本人/紧急联系电话 |
| | `district` | enum | ✅ | 所属区县（字典） |
| | `address` | string | | 居住地址 |
| | `orgId` | string | | 归属养老机构/社区 |
| | `abilityLevel` | enum | | 能力评估等级（关联 G12） |
| | `tags` | array | | 标签（独居/高龄/失能/低保…字典） |
| | `family` | array | | 家属联系人（见下） |
| | `health` | object | | 健康档案（见下） |
| | `createdBy`/`at` | | ✅ | 建档人/时间 |
| `elderFamily` | `name`/`relation`/`phone` | | ✅ | 家属（relation 字典） |
| `elderHealth` | `bloodType`/`chronic`[]/`allergy`[]/`height`/`weight` | | | 血型/慢病/过敏史 |

#### 4. 核心业务流程
```
建档(导入Excel/手工) ──► 基本信息 ──► 关联家属 ──► 健康档案 ──► 标签/评估
                                                              │
                         工单(G06)/监测(G09)/评估(G12)/补贴(G11) ──┘ 反向填充服务记录
```
- **唯一性**：idCard 去重校验；支持 Excel 批量导入（复用 G07 文件上传 + 模板下载）。
- **级联**：`district` 决定区县管理员可见范围；`orgId` 关联现有 `institutions`/`facilities`。

#### 5. 页面设计
| 页面 | 路由 | 布局 |
|---|---|---|
| 档案列表 | `/admin/elders` | 通用列表骨架 + 筛选（区县/能力等级/标签/关键字）；行操作：查看/编辑/服务记录 |
| 档案详情 | 抽屉/独立页 | **Tab 分段**：① 基本信息（左卡片 + 右标签云）② 健康档案（血型/慢病/过敏 + 体征入口）③ 家属联系人（卡片列表）④ 服务记录（关联工单 G06 时间线）⑤ 评估记录（关联 G12） |
| 新建/编辑 | Modal | 分步表单：基本信息 → 家属（可多） → 健康 → 标签 |

**关键交互**：① 列表支持按能力等级色标（失能红/半失能橙/正常绿）；② 详情 Tab 间数据联动（如健康 Tab 内"查看趋势"跳 G09）；③ 批量导入带校验报告。

---

### G04　数据字典管理

**优先级：高　|　业务价值：消除硬编码枚举，保证全系统口径一致，变更无需发版**

#### 1. 功能概述
统一维护全系统枚举值（机构类型 / 设备类型 / 告警分类 / 养老方式 / 评估等级 / 性别 / 亲属关系等），前后端共享、动态下拉。当前这些枚举散落在各模块代码中（如 `CAT`、`level` 字符串），本模块将其集中治理。

#### 2. 角色与权限
仅超级管理员 / 系统配置角色可编辑；其余只读。

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `dictTypes` | `type` | string | ✅ | 字典类型编码（如 `elderAbility`） |
| | `name` | string | ✅ | 类型名称（如"能力评估等级"） |
| | `remark` | string | | 说明 |
| `dictItems` | `type` | string | ✅ | 关联 dictTypes.type |
| | `code` | string | ✅ | 值编码（如 `L3`） |
| | `label` | string | ✅ | 显示名（如"重度失能"） |
| | `sort` | int | ✅ | 排序 |
| | `status` | enum | ✅ | 启用/停用 |
| | `color` | string | | 可选色标（用于等级着色，如失能红） |

#### 4. 核心业务流程
```
字典类型列表 ──► 选中类型 ──► 该类型字典项表格(CRUD)
      │                                  │
      └── 新增类型 ──► 同步生成空项容器 ──┘
前端/后端读取字典 ──► 动态下拉/标签渲染（缓存 + 版本号失效）
```

#### 5. 页面设计
左树（字典类型）+ 右表（选中类型的字典项）经典主从布局；项表格支持启停开关、拖拽排序、色标选择。提供「导出 JSON / 同步到前端」按钮（生成 `src/dict/index.js` 常量供前端 import，避免每次请求）。

---

### G05　定时任务调度

**优先级：高　|　业务价值：当前数据同步全靠手动，缺乏自动化；养老场景需定时采集设备数据、定时推送**

#### 1. 功能概述
可视化 Cron 任务管理：数据同步 / 报表生成 / 设备巡检 / 补贴核算 / 消息推送。支持启停、手动触发、执行日志、失败重试、通知。

#### 2. 角色与权限
超级管理员 / 运维角色可管理；其余只读。

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `jobs` | `id` | string | ✅ | 任务编码 |
| | `name` | string | ✅ | 任务名 |
| | `group` | enum | ✅ | 数据同步/报表/巡检/核算/推送 |
| | `cron` | string | ✅ | Cron 表达式 |
| | `target` | string | ✅ | 执行目标（如 `sync:medical`） |
| | `params` | object | | 参数 |
| | `status` | enum | ✅ | 运行中/已暂停/异常 |
| | `failRetry` | int | ✅ | 失败重试次数 |
| | `notify` | array | | 失败通知渠道（复用 notifyConfig） |
| | `lastRun`/`nextRun` | datetime | | 上次/下次执行 |
| `jobLogs` | `jobId`/`start`/`end`/`result`/`detail` | | ✅ | 执行日志 |

#### 4. 核心业务流程
```
任务列表 ──► 启停开关
   │
   ├─ 手动[触发] ──► 立即执行 ──► 写 jobLogs(成功/失败)
   │
   └─ 调度器(每分扫描) ──► 命中 cron ──► 执行 ──► 失败≤retry重试 ──► 仍败→notify告警(G04)
```
- **Cron 编辑器**：可视化生成表达式（分/时/日/月/周 + 常用模板：每日0点/每小时/每周一）。

#### 5. 页面设计
| 页面 | 路由 | 布局 |
|---|---|---|
| 任务列表 | `/admin/jobs` | 通用列表 + 启停开关列 + [触发][日志] 行操作；状态色标 |
| 任务编辑 | Modal | 名称/分组/Cron(可视化编辑器)/目标/参数/重试/通知 |
| 执行日志 | 抽屉 | 选中任务 → 时间线式日志（执行时长/结果/错误栈折叠） |

---

## 第二阶段 · 业务闭环与安全审计

### G06　服务工单中心　★业务主链路

**优先级：高　|　业务价值：养老服务运营核心载体，当前 orders 偏静态记录，缺业务闭环**

#### 1. 功能概述
工单全生命周期管理：派单 → 接单 → 上门签到 → 服务 → 评价 → 结算。含工单池、调度看板、工单类型与优先级。关联老人档案（G03）、最终结算接入财务（G11）。

#### 2. 角色与权限
| 角色 | 权限 |
|---|---|
| 调度管理员 | 建单/派单/改派/查看全部 |
| 服务专员 | 接单/签到/完成/上传服务记录 |
| 机构操作员 | 本机构工单 |

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `workOrders` | `id` | string | ✅ | 工单号（WO+日期+序号） |
| | `elderId` | string | ✅ | 关联 elders（G03） |
| | `type` | enum | ✅ | 工单类型（字典：助洁/助餐/护理/维修…） |
| | `priority` | enum | ✅ | 紧急/高/中/低（字典） |
| | `status` | enum | ✅ | 见状态机 |
| | `assignee` | string | | 接单专员 |
| | `address` | string | ✅ | 服务地址（默认老人住址） |
| | `createdAt`/`acceptedAt`/`arrivedAt`/`doneAt`/`ratedAt` | datetime | | 各节点时间 |
| | `serviceItems` | array | | 服务项目/数量 |
| | `photos` | array | | 服务前/后照片（G07） |
| | `rating` | int | | 评价星数 1–5 |
| | `comment` | string | | 评价内容 |
| | `fee` | number | | 费用（结算 G11） |
| | `settledAt` | datetime | | 结算时间 |

#### 4. 核心业务流程（状态机）
```
         [新建]
           │
           ▼
      ┌──────────┐   派单    ┌──────────┐   接单    ┌──────────┐
      │ 待派单    │ ───────► │ 已派单    │ ───────► │ 已接单    │
      └──────────┘          └──────────┘          └────┬─────┘
                                                         │ 上门签到
                                                         ▼
                                                  ┌──────────┐   完成   ┌──────────┐
                                                  │ 服务中    │ ───────► │ 待评价    │
                                                  └──────────┘          └────┬─────┘
                                                                            │ 评价
                                                                            ▼
                                                                     ┌──────────┐  结算  ┌──────────┐
                                                                     │ 已完成    │ ──────► │ 已结算    │
                                                                     └──────────┘       └──────────┘
       任意节点(除已结算) ──► [取消] ──► 已取消（需填原因）
       已派单超时未接 ──► 自动回流工单池（预警 G04）
```

#### 5. 页面设计
| 页面 | 路由 | 布局 |
|---|---|---|
| 工单池 | `/admin/orders/pool` | 待认领工单列表，[一键认领]；地图分布（复用 BigScreen 点位思路） |
| 调度看板 | `/admin/orders/board` | **看板视图**：按状态分列（待派单/已派单/已接单/服务中/待评价），卡片拖拽流转；顶部统计各列计数 |
| 工单列表 | `/admin/orders` | 通用列表 + 状态/类型/优先级筛选 + 时间范围 |
| 工单详情 | 抽屉 | 老人信息卡 + 状态时间线 + 服务记录 + 照片 + 评价 + [派单/接单/完成/结算] 动作 |

**关键交互**：① 看板拖拽改状态（乐观更新 + 后端确认）；② 紧急工单红框闪烁；③ 详情时间线节点高亮当前态；④ 派单支持按专员负载智能推荐。

---

### G07　文件与附件管理

**优先级：中　|　业务价值：多模块共用基础设施，老人档案/内容配图/合同归档均依赖**

#### 1. 功能概述
统一文件上传存储与关联：老人照片 / 证件 / 合同 / 内容配图 / 评估报告。支持存储配额、缩略图预览、与业务实体关联。

#### 2. 角色与权限
上传者可读写自己文件；管理员可管理全部；业务模块通过 `relatedType+relatedId` 关联。

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `files` | `id` | string | ✅ | 文件 ID |
| | `name` | string | ✅ | 原始名 |
| | `type` | enum | ✅ | 图片/文档/视频 |
| | `mime`/`size` | | ✅ | 类型/大小 |
| | `bucket`/`url` | string | ✅ | 存储桶/访问地址 |
| | `relatedType` | string | | 关联实体（elder/assessment/content…） |
| | `relatedId` | string | | 关联 ID |
| | `uploadedBy`/`at` | | ✅ | 上传人/时间 |

#### 4. 核心业务流程
```
业务模块[上传] ──► 选择/拖拽文件 ──► 后端存储+生成缩略图 ──► 返回 url + 写 files(关联)
                                                                  │
查看/下载/删除 ──► 校验关联权限 ──► 操作
```
- **配额**：按 `relatedType` 限张数（如老人头像 1、证件 4）；超限提示。

#### 5. 页面设计
`/admin/files`：左分类（全部/图片/文档/视频/未关联）+ 右网格（缩略图卡片，hover 显示关联对象与操作）；上传区支持拖拽；点击预览大图/视频。

---

### G08　报表中心

**优先级：中　|　业务价值：监管汇报与决策支撑，民政月报/季报刚需**

#### 1. 功能概述
养老业务统计报表（机构/社区/居家/医疗维度）、政府补贴报表、自定义报表、导出打印。可定时生成（依赖 G05）。

#### 2. 角色与权限
管理员可设计并生成；区县仅看本区；支持导出 PDF/Excel。

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `reportTpls` | `id`/`name`/`category`/`dimensions`/`filters`/`chartType` | | ✅ | 报表模板 |
| `reportTasks` | `tplId`/`params`/`generatedAt`/`fileUrl`/`status` | | ✅ | 生成记录 |

#### 4. 核心业务流程
```
模板库 ──► 选模板 + 填参数(时间/区县) ──► [生成] ──► 预览(图表/表格) ──► 导出PDF/Excel
                            │
               定时任务(G05) 自动生成 ──► 推送消息(G13)
```

#### 5. 页面设计
`/admin/reports`：左模板树（按业务域分组）+ 右生成区（参数表单 + 预览面板，复用 `useChart`）；历史生成记录列表可下载。

---

### G09　健康监测管理

**优先级：中　|　业务价值：慢病管理与主动预警，与告警中心联动，提升服务温度**

#### 1. 功能概述
老人健康数据采集（血压/心率/血糖/跌倒/睡眠）、趋势分析、个性化预警阈值、异常自动告警接入（G04 告警中心）。依赖 G03 老人 + G05 定时采集。

#### 2. 角色与权限
健康管理员可配置阈值与查看；机构操作员看本机构；老人数据仅本人及家属可见（隐私）。

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `healthMetrics` | `elderId` | string | ✅ | 关联老人 |
| | `type` | enum | ✅ | 体征类型（字典：血压/心率/血糖…） |
| | `value` | number/string | ✅ | 值（血压存"120/80"） |
| | `unit` | string | ✅ | 单位 |
| | `measuredAt` | datetime | ✅ | 测量时间 |
| | `deviceId` | string | | 来源设备（G05 物联） |
| | `level` | enum | ✅ | 正常/异常（比对阈值） |
| `healthThresholds` | `type`/`min`/`max`/`elderScope` | | ✅ | 预警阈值（全局/个人） |

#### 4. 核心业务流程
```
设备/手工采集 ──► 写入 healthMetrics ──► 比对阈值 ──► 异常 ──► 生成告警(G04.alertData) + 通知(G13)
                                          │
                                    正常 ──► 趋势归档
老人档案(G03) ──► 查看体征趋势图(按 type 时间线) ──► 异常高亮
```

#### 5. 页面设计
`/admin/health`：① 监测总览（异常数 KPI + 今日采集量）；② 老人体征查询（选老人 → 多体征趋势图 Tab）；③ 异常列表（联动告警中心）；④ 阈值配置（全局 + 按老人覆盖）。

---

### G10　登录日志与安全审计

**优先级：中　|　业务价值：合规审计，当前操作日志含"登录"动作但无独立视图与安全策略**

#### 1. 功能概述
独立登录日志（成功/失败/IP/设备/地理位置）、异常行为标记、暴力破解防护策略配置。与 G01 认证、G02 个人中心联动。

#### 2. 角色与权限
仅安全/超级管理员可查看与配置。

#### 3. 数据模型
| 实体 | 字段 | 类型 | 必填 | 说明 |
|---|---|---|:---:|---|
| `loginLogs` | `id`/`userId`/`username` | | ✅ | 用户 |
| | `ip`/`device`/`location` | string | | 来源 |
| | `result` | enum | ✅ | 成功/失败/锁定 |
| | `failReason` | string | | 失败原因 |
| | `time` | datetime | ✅ | 时间 |

#### 4. 核心业务流程
```
登录事件(G01) ──► 写 loginLogs(结果/IP/设备)
                       │
           失败≥maxFail ──► 触发锁定 + 标红 + 可选告警
异常登录(异地/新设备) ──► 标记 + 通知(G13)
```

#### 5. 页面设计
`/admin/login-logs`：通用列表（结果色标：成功绿/失败红/锁定橙）+ 筛选（用户/结果/时间/IP）；详情抽屉（完整上下文）；`安全策略`Tab（锁定阈值/验证码开关/异地提醒，写 `authConfig`）。

---

*卷一结束（G01–G10）。卷二见《后台功能详细设计-卷二-G11-G18.md》。*
